# <任务名>

```
status: draft | frozen | done | aborted
version: 1
created: YYYY-MM-DD
pipeline: herdr-agent | subagent-worker | self   # 受托管道;self=管家自做(低于委托线)
```

## 背景与目标

（为什么做、做成什么样。引用 second-brain 笔记名承载个人体系背景，不内联细节。）

## 决策记录

（深挖期每个决策当场编号落这里，聊天里飘着不算数。）

- D1: ……（理由）
- D2: ……（理由）

## 数据源 / 上下文（自包含）

（受托方零上下文可执行所需的精确路径、格式、约定。）

## 硬性约束

- 改动白名单：老文件只许动 `X`、`Y`；其余一律新文件
- 只读边界：……（禁止写/删的路径）
- 禁令：禁跑 build（归父会话）等

## 验收标准

- ……

## 结果记录（done 时填）

- commit(s): `<hash>`
- 受托方交付总结要点 / 遗留决策点处置：
- 回流的决策（升版记录）：
